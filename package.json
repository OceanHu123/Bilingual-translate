{
  "name": "bilingual-translate",
  "version": "0.1.14",
  "private": true,
  "type": "module",
  "description": "Bilingual webpage translation that skips code blocks, especially on Ed.",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.2.6",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
